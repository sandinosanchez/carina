pipeline {
    agent { label 'master' }
    stages {
        stage('test') {
            steps {
                echo "Hello World!"
                sh "echo Hello from the shell"
            }
        }
    }
}
